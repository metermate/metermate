angular
  .module('metermate.my-meters', [])
  .controller('myMetersCtrl', function($scope) {
    $scope.testing = 'My Meter is connected' //Change this; This is to check that the route is working;
  });

//Bonus feature 😬
